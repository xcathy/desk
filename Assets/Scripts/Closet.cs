using UnityEngine;

public class Closet : MonoBehaviour, IObject
{
    public void Grab()
    {
        Debug.Log("closet grabbed");
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
